#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec julia --project="${PROJECT_ROOT}" -e 'using Epsilon; exit(Epsilon.pipeline_main())' -- "$@"
