---
phase: 16-scenario-planner-manual-allocation
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-07-05T00:00:00.000Z
---

<current_state>
Phase 16 is planned. Tasks 16-01, 16-02, and 16-03 are landed. Task 16-04 is
next.
</current_state>

<completed_work>
- Phase 16 plan written in
  `.planning/phases/16-scenario-planner-manual-allocation/PLAN.md`.
- `.planning/ROADMAP.md` and `.planning/STATE.md` now point at Phase 16.
- Task 16-01 added `ManualScenarioEvaluationResult` and
  `evaluate_manual_scenario(results, scenario)` for bounded time-series manual
  allocation evaluation over existing response surfaces.
- Omitted channels are held at observed spend; invalid channels, zero-total
  evaluated budgets, and out-of-domain response-surface spends fail closed.
- Task 16-02 added `scenario_plan` overloads for one or more
  `ManualScenarioEvaluationResult` values, projecting current plus manual rows
  into `ScenarioPlanResult` totals, channel, allocation, and metadata tables.
- The existing optimizer-backed `scenario_plan(::BudgetOptimizationResult)`
  output remains backward compatible.
- Task 16-03 added `scenario_plan(result, evaluation)` and
  `scenario_plan(result, evaluations)` overloads that combine current,
  manual-allocation, and solved fixed-budget optimized rows in one
  `ScenarioPlanResult`.
- The combined path hard-rejects mismatched artifact metadata, model spec,
  coordinate metadata, objective, current spend, current response, and current
  default efficiency before table construction.
</completed_work>

<next_step>
Implement Task 16-04: close docs, changelog, and ledger guardrails for the
bounded Phase 16 manual-allocation scenario-planner surface without claiming
Dash/UI, scenario-store, automatic-refit, or panel manual-allocation support.
</next_step>

<verification_status>
Latest Task 16-03 verification passed:

```bash
julia --project=@runic -m Runic --check --diff src/scenario_planner.jl test/scenario_planner.jl
JULIA_PKG_SERVER_REGISTRY_PREFERENCE=eager julia --project=. -e 'using Pkg; Pkg.test(; test_args=["scenario_planner"])'
git diff --check
```

The scenario-planner lane reported `Pass 88, Total 88`.
</verification_status>

<remaining_work>
- Task 16-04: docs/changelog/ledger guardrails.
</remaining_work>

<known_gotchas>
- Keep this phase non-UI and no-refit.
- Do not add panel manual-allocation semantics without a separate
  historical-share contract.
- Do not run the full suite for routine scenario-planner iteration; use the
  targeted scenario-planner test lane and targeted Runic checks.
</known_gotchas>
