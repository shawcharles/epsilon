---
phase: 14-abacus-parity-recovery
task: 5
total_tasks: 5
status: completed
last_updated: 2026-05-20T06:50:00.000Z
---

<current_state>
Phase 13 contract/remediation and Phase 14 Abacus parity recovery are closed
for the bounded v1 evidence spine. Plans 14-01 through 14-05 are implemented.
Plan 14-05 is closed and covers the `timeseries` pipeline artifact-key contract
through the bounded Stage `00`-`70` surface plus `geo_panel` and
`geo_brand_panel` pipeline Stage `00` metadata/manifest, Stage `20` fit, and
Stage `30` assessment, Stage `40` decomposition, and Stage `50` diagnostics
plus Stage `60` response-curve artifact-key parity. Both `geo_panel` and
`geo_brand_panel` now also have explicitly enabled Stage `70`
historical-share optimization artifact paths.

Phase 13 contract/remediation issues have also been closed and revalidated on
2026-05-20. The fitted time-series trend and automatic-holiday date-basis state
is serialized into model specs and reused for prediction/replay; unfitted
time-series prior prediction resolves scale and date-derived state from
`model.data`; media/channel arrays are rejected when negative; `hill_function`
rejects negative inputs with a clear `ArgumentError`; and pipeline YAML now
rejects unknown top-level keys instead of silently ignoring typoed run blocks.

State was saved on 2026-05-20 for a pause. On resume, do not restart Phase 14
pipeline plumbing. The next useful move is to choose an explicit release-prep
slice: clean benchmark snapshot, bounded non-UI scenario-planner expansion
such as manual-allocation response evaluation or saved scenario-store
artifacts, or a separately specified prior-sensitivity execution contract.

The current boundary is:

- `timeseries` config/data fixture spine: accepted.
- `timeseries` deterministic model/replay gate: accepted.
- `timeseries` pipeline Stage `00` through Stage `70` artifact-key parity:
  accepted for manifest keys and stage-local artifacts, with Julia-native
  serialized artifacts replacing Abacus PyMC/NetCDF-specific files where file
  identity would be misleading.
- `geo_panel` config/model/replay gate: accepted for deterministic replay.
- `geo_brand_panel` multidimensional config/model/contribution/decomposition
  replay: accepted.
- `geo_brand_panel` panel-cell response/metric semantics: accepted with
  explicit `delta_grid` historical scaling.
- `geo_panel` pipeline Stage `00` metadata/manifest parity, Stage `20` fit
  artifact-key parity, Stage `30` assessment artifact-key parity, and Stage
  `40` decomposition artifact-key parity, and Stage `50` diagnostics
  artifact-key parity, plus Stage `60` response-curve artifact-key parity:
  accepted for panel-aware metadata, Julia-native fit artifacts, posterior
  summaries, trace plots, observed/fitted/residual tables, posterior
  predictive summaries, assessment plots, typed contribution/decomposition
  artifacts, panel-aware decomposition summaries, Abacus-compatible
  contribution CSVs, decomposition/media contribution plots, panel-aware design
  reports, chain/MCMC diagnostics, predictive and residual diagnostics, VIF
  reports, residual ACF plots, and panel-cell historical-scaling response,
  saturation, adstock, metric, summary, and plot artifacts, with unsupported
  later panel stages explicitly skipped unless Stage `70` optimization is
  explicitly enabled.
- `geo_panel` and `geo_brand_panel` pipeline Stage `70` historical-share
  optimization: accepted for the bounded v1 policy that optimizes channel
  totals, reuses panel response curves through shared channel deltas, and
  preserves historical within-channel panel-cell spend shares. For
  `geo_brand_panel` this includes multidimensional `geo`/`brand` coordinate
  metadata in the emitted `channel_panel_allocation.csv` and
  `channel_delta_audit.csv` artifacts. Free channel-by-panel allocation,
  panel-total bounds, fairness constraints, and weighted objectives remain
  deferred.
- Stage `35` panel holdout validation: explicitly deferred for v1. Epsilon
  keeps time-series blocked holdout validation, but does not add panel holdout
  validation without a concrete methodological requirement and a separate
  fixture-backed contract.
- Post-model result axis contracts are now locked at the summary/metric
  boundary. Contribution, decomposition, response, saturation, adstock, and
  metric artifacts validate their draw, panel, spend-point, component, and
  metric axes before table or downstream metric derivation. Panel curve and
  metric artifacts require `(panel, spend_point)` spend grids, explicit
  `delta_grid` historical multipliers, and `panel_cell` coordinate metadata.
- Response, saturation, and adstock curve result construction now flows
  through one shared internal builder. This is a structural cleanup only:
  time-series and panel replay math, public entrypoints, and emitted result
  schemas are unchanged.
- `geo_brand_panel` pipeline Stage `00` metadata/manifest parity, Stage `20`
  fit artifact-key parity, Stage `30` assessment artifact-key parity, and
  Stage `40` decomposition artifact-key parity, and Stage `50` diagnostics
  artifact-key parity, plus Stage `60` response-curve artifact-key parity:
  accepted for multidimensional panel-cell metadata, coordinate round trips,
  Julia-native fit artifacts, posterior summaries, trace plots,
  observed/fitted/residual tables, posterior predictive summaries, and
  assessment plots, typed contribution/decomposition artifacts, panel-aware
  decomposition summaries, Abacus-compatible contribution CSVs, and
  decomposition/media contribution plots, panel-aware design reports,
  chain/MCMC diagnostics, predictive and residual diagnostics, VIF reports, and
  residual ACF plots, panel-cell historical-scaling response, saturation,
  adstock, metric, summary, and plot artifacts, with unsupported later panel
  stages explicitly skipped.
</current_state>

<completed_work>

- Exported the local Abacus `timeseries` pipeline manifest/artifact contract
  into `test/fixtures/abacus/timeseries/config_data.jl`.
- Added Stage `00` metadata parity artifacts and manifest checks.
- Added Stage `20` fit artifact-key parity, mapping Abacus `idata` to
  Epsilon's Julia-native grouped `InferenceResults` artifact.
- Added Stage `30` assessment parity artifacts for posterior predictive
  summaries and fit/residual diagnostic plots.
- Added Stage `35` holdout validation parity keys:
  `holdout_observed`, `holdout_fitted`, `holdout_residuals`,
  `holdout_posterior_predictive`, `holdout_predictive_summary`,
  `holdout_predictive_report`, and `holdout_residuals_acf_plot`.
- Added Stage `40` decomposition parity keys:
  `baseline_contributions`, `channel_contributions`,
  `mean_contributions_over_time`, `waterfall_plot`, and
  `weekly_media_contribution_plot`.
- Added Stage `50` diagnostics parity keys:
  chain/design/MCMC/predictive/residual/VIF reports plus residual ACF plot.
- Added Stage `60` curves parity keys:
  bundled response, saturation, and adstock artifacts, summaries, and plots.
- Added enabled Stage `70` optimization parity keys:
  budget summary, impact, bounds, allocation, ROI/MROI, response, and plot
  artifacts.
- Exported the local Abacus `geo_panel` pipeline manifest/artifact contract
  into `test/fixtures/abacus/geo_panel/config_data.jl`.
- Added a panel metadata/fit pipeline path that loads combined panel CSVs,
  writes Stage `00` panel dataset/model metadata, runs Stage `20` panel fit,
  and marks unsupported panel stages skipped.
- Added `geo_panel` Stage `00` and Stage `20` pipeline manifest/artifact-key
  tests.
- Added `geo_panel` Stage `30` assessment manifest/artifact-key tests with
  panel-aware observed/fitted, residual, posterior predictive summary, and plot
  artifacts.
- Added `geo_panel` Stage `40` decomposition manifest/artifact-key tests with
  panel-aware contribution/decomposition summaries, Julia-native
  `ContributionResults` and `DecompositionResults` artifacts, Abacus-compatible
  contribution CSVs, and decomposition/media contribution plots.
- Added `geo_panel` Stage `50` diagnostics manifest/artifact-key tests with
  panel-aware design reports, chain/MCMC diagnostics, predictive diagnostics,
  residual diagnostics, VIF reports, and residual ACF plots.
- Added `geo_panel` Stage `60` response-curve manifest/artifact-key tests with
  panel-cell `delta_grid` response, saturation, adstock, metric, summary, and
  plot artifacts.
- Added `geo_panel` Stage `70` historical-share optimization manifest/
  artifact-key tests that load a `PanelBudgetOptimizationResult`, assert the
  bounded `panel_allocation_mode = :historical_shares`, and check that
  optimized channel-panel spend sums back to the optimized channel total.
- Added `geo_brand_panel` Stage `70` historical-share optimization
  manifest/artifact-key tests that exercise the same bounded policy across
  flattened multidimensional panel-cell axes and assert that `geo` and `brand`
  coordinate columns survive in the `channel_panel_allocation.csv` and
  `channel_delta_audit.csv` artifacts.
- Exported the local Abacus `geo_brand_panel` pipeline manifest/artifact
  contract into `test/fixtures/abacus/geo_brand_panel/config_data.jl`.
- Added `geo_brand_panel` Stage `00` and Stage `20` pipeline
  manifest/artifact-key tests for multidimensional panel metadata and
  serialized fit artifacts.
- Added `geo_brand_panel` Stage `30` assessment manifest/artifact-key tests
  with multidimensional panel coordinate columns in assessment CSV artifacts.
- Added `geo_brand_panel` Stage `40` decomposition manifest/artifact-key tests
  with multidimensional panel coordinate columns in contribution summaries and
  Abacus-compatible decomposition artifact keys.
- Added `geo_brand_panel` Stage `50` diagnostics manifest/artifact-key tests
  with multidimensional panel design summaries and panel-aware diagnostics
  artifacts under Abacus-compatible keys.
- Added `geo_brand_panel` Stage `60` response-curve manifest/artifact-key tests
  with multidimensional panel coordinate columns in curve summaries and
  Abacus-compatible curve artifact keys.
- Tightened `test/pipeline/run.jl` so the exported Abacus stage artifact-key
  sets for supported stages are subsets of the Epsilon manifest artifact paths
  and all mapped files exist.
- Updated `README.md`, `docs/src/index.md`, `docs/src/release.md`,
  `.planning/ABACUS-PARITY-LEDGER.md`,
  `.planning/phases/14-abacus-parity-recovery/PLAN.md`, and
  `.planning/STATE.md` to state the new boundary.
- Closed Phase 13 contract/remediation issues for fitted trend/holiday replay
  state, nonnegative media-domain validation, `hill_function` negative-input
  validation, strict top-level pipeline YAML keys, and release-gate
  revalidation.
- Updated `AGENTS.md`, `CHANGELOG.md`, `.planning/STATE.md`,
  `.planning/ROADMAP.md`, and this continuation handoff for the 2026-05-20
  pause.
</completed_work>

<remaining_work>

- Expand scenario planner semantics only behind a concrete non-UI planning
  contract, such as manual-allocation response evaluation or saved scenario
  store artifacts. The first bounded spec/table surface over solved optimizer
  outputs is now implemented.
- Keep prior sensitivity scoped to scenario-config and manifest planning unless
  a separate multi-fit comparison contract is designed.
- Refresh the benchmark snapshot from a clean worktree before publishing a
  release artifact.
- Keep AI advisor and Dash/dashboard parity deferred.
- Do not claim full Abacus parity for panel pipeline orchestration until
  fixture-backed tests exist.
- Do not fake NetCDF/PyMC identity; continue using honest Julia-native
  artifacts where the semantics match.
</remaining_work>

<decisions_made>

- Preserve Abacus manifest/artifact-key semantics where they are meaningful.
- Prefer typed Julia-native `.jls` artifacts over fake NetCDF files for PyMC
  or ArviZ-specific Abacus outputs.
- Treat panel response/metric artifacts as panel-cell/channel surfaces with an
  explicit `delta_grid` historical-scaling contract.
- Defer aggregate panel budget allocation semantics until there is an explicit
  allocation policy.
- AI advisor and Dash/dashboard parity remain out of v1 scope.
</decisions_made>

<blockers>

- Repo-wide `make format-check` still fails on pre-existing Runic drift across
  older files outside this slice. Targeted Runic checks on touched Julia files
  passed.
- Release branch/tag work remains blocked on Phase 14 demo-backed acceptance.
- Broad Abacus parity claims remain blocked on ledger-backed demo acceptance.
</blockers>

<validation>

Last completed checks:

- `julia --project=. test/scenario_planner.jl` passed.
- `julia --project=@runic -m Runic --check --diff src/Epsilon.jl src/scenario_planner.jl test/scenario_planner.jl test/runtests.jl`
  passed.
- `git diff --check` passed.
- `make docs` passed, with existing Documenter warnings about generated
  `index.html` size and skipped deployment environment detection.
- `make test` passed with 3692 assertions in 18m40.6s.

- `julia --project=. test/model/trend.jl && julia --project=. test/model/types.jl && julia --project=. test/transforms/saturation.jl && julia --project=. test/pipeline/config.jl`
  passed for the Phase 13 focused contract regressions.
- `julia --project=. test/model/builder.jl` passed, including fitted
  trend/holiday replay-state checks.
- `julia --project=. test/model/runtests.jl` passed; direct standalone
  inference sub-suite invocation remains unsuitable because it depends on
  helpers loaded by `test/runtests.jl`.
- `julia --project=. test/pipeline/run.jl` passed, including time-series,
  `geo_panel`, and `geo_brand_panel` pipeline flows.
- `julia --project=@runic -m Runic --check --diff src/mmm/trend.jl src/mmm/holidays.jl src/mmm/model.jl src/model/builder.jl src/model/types.jl src/pipeline/config.jl src/transforms/saturation.jl test/model/trend.jl test/model/types.jl test/model/builder.jl test/pipeline/config.jl test/transforms/saturation.jl`
  passed.
- `make docs` passed, with the existing Documenter warnings about generated
  `index.html` size and skipped deployment environment detection.
- `git diff --check` passed.
- `make test` passed with 3786 assertions in 21m11.4s.
- Repo-wide `make format-check` was rerun and still fails on pre-existing
  Runic drift outside the Phase 13 remediation slice (for example
  `src/distributions/*`, older model/config tests, plotting tests, and
  transform tolerance literal formatting). The targeted Runic check above
  passed for the touched Julia files.

- `PYTHONNOUSERSITE=1 python scripts/export_abacus_fixtures.py` passed.
- Direct `geo_panel` metadata/fit/assessment `run_pipeline(...)` smoke check
  passed before the Stage `40` slice.
- Direct `geo_brand_panel` metadata/fit/assessment coverage passed through
  `test/pipeline/run.jl` before the Stage `40` slice.
- `julia --project=. -e 'using Test, Epsilon; include("test/validation/parity.jl"); include("test/validation/geo_panel_config_data.jl")'` passed.
- `julia --project=. test/pipeline/run.jl` passed, including `geo_panel`
  Stage `00`/`20`/`30`/`40`/`50`/`60` with 192 assertions and
  `geo_brand_panel` Stage `00`/`20`/`30`/`40`/`50`/`60` with 199 assertions.
- `julia --project=. test/pipeline/cli.jl` passed.
- `make docs` passed, with existing Documenter warnings about generated
  `index.html` size and skipped deployment environment detection.
- `git diff --check` passed.
- `julia --project=@runic -m Runic --check src/pipeline/stages.jl test/pipeline/run.jl`
  passed.
- Full `make test` was not rerun after the Stage `60` panel response-curve
  slice; it passed earlier in the session before the panel assessment slice
  with 3311 assertions in 15m56s.
- Repo-wide `make format-check` was rerun and remains blocked by pre-existing
  Runic drift outside this slice; use the targeted Runic check above for the
  Phase 13 remediation files.
</validation>

<files_modified_this_slice>

- `src/pipeline/stages.jl`
- `src/pipeline/config.jl`
- `src/pipeline/context.jl`
- `src/mmm/trend.jl`
- `src/mmm/holidays.jl`
- `src/mmm/model.jl`
- `src/model/builder.jl`
- `src/model/types.jl`
- `src/transforms/saturation.jl`
- `test/model/trend.jl`
- `test/model/types.jl`
- `test/model/builder.jl`
- `test/pipeline/config.jl`
- `test/transforms/saturation.jl`
- `test/pipeline/run.jl`
- `test/pipeline/cli.jl`
- `src/scenario_planner.jl`
- `test/scenario_planner.jl`
- `test/runtests.jl`
- `scripts/export_abacus_fixtures.py`
- `test/fixtures/abacus/geo_panel/config_data.jl`
- `test/fixtures/abacus/geo_brand_panel/config_data.jl`
- `test/fixtures/abacus/README.md`
- `README.md`
- `docs/src/index.md`
- `docs/src/release.md`
- `AGENTS.md`
- `CHANGELOG.md`
- `.planning/ABACUS-PARITY-LEDGER.md`
- `.planning/phases/14-abacus-parity-recovery/PLAN.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`

The worktree also contains many broader modified/untracked files from earlier
project work. Do not revert unrelated files.
</files_modified_this_slice>

<context>
Stage `70` historical-share optimization is now fixture-backed for both
`geo_panel` and `geo_brand_panel`. Stage `35` panel validation is deferred for
v1, and free/aggregate panel budget semantics should remain out until
fixture-backed. Plan `14-05` is closed with the parity audit recorded in
`.planning/ABACUS-PARITY-LEDGER.md`. The scenario planner now has a bounded
non-UI spec/table surface over solved optimization results.
</context>

<next_action>
Choose the next scenario-planner slice only if it has an explicit non-UI
contract, such as manual-allocation response evaluation or persisted scenario
store artifacts. Automatic prior-sensitivity multi-fit comparison remains
outside the bounded Stage `05` planning contract.
</next_action>
