---
phase: 12-parity-remediation
task: 4
total_tasks: 4
status: completed
last_updated: 2026-04-24T11:20:55.850Z
---

<current_state>
Phase 12 is complete and the roadmap is closed again. The repository now has a
truthful release gate:

- `VAL-TS-00-MCMC` is the guaranteed Abacus-reference row
- `VAL-TS-04-MCMC` is retained as a bounded Epsilon-native/reference row on
  the native automatic holiday contract
- the release/docs/planning layer has been reconciled to that narrowed claim

The external methodology audit has been moved from repo root to
`.planning/METHODOLOGY_AUDIT.md`.
</current_state>

<completed_work>

- Task 1: Scaling and model-space parity - Done
- Task 2: Stage 60 curve-family parity - Done
- Task 3: Coherent native holiday/demo design - Done
- Task 4: Revalidation and release reconciliation - Done
  - split `test/validation/parity.jl` into one strict Abacus-reference row and
    one bounded holiday-bearing native-reference row
  - updated `test/fixtures/abacus/validation/ts04_mcmc/config.yml` to the
    native pooled automatic holiday contract and added
    `test/fixtures/abacus/validation/ts04_mcmc/holidays.csv`
  - reconciled release/docs/planning files so Phase 12 is marked complete and
    the release claim is narrowed truthfully
  - moved `METHODOLOGY_AUDIT.md` to `.planning/METHODOLOGY_AUDIT.md`
</completed_work>

<remaining_work>

- Phase 12: no remaining implementation work
- Roadmap: complete
- Practical next step: release preparation from the closed bounded v1 surface
- Release hygiene still advisable:
  - rerun frozen benchmarks from a clean worktree so published artifacts no
    longer carry `git_dirty = true`
  - then prepare release branch / tag / notes
</remaining_work>

<decisions_made>

- Abacus is a major reference and comparison baseline, but methodological
  coherence wins over literal fidelity when the two conflict.
- The guaranteed Abacus-reference release-gate row is `VAL-TS-00-MCMC`.
- The holiday-bearing automatic-holiday row is not claimed as Abacus parity; it
  is an Epsilon-native/reference row unless a separate compatibility mode is
  added later.
- Native holidays use one pooled automatic component (`holidays.mode = "auto"`)
  and manual named holiday treatments belong under `events`.
- Stage-local pipeline plots plus post-hoc `write_plot_bundle(run)` remain the
  frozen plotting contract.
</decisions_made>

<blockers>
- No implementation blocker remains for the roadmap itself.
- The only outstanding release-quality note is the published benchmark snapshot
  still reflecting `git_dirty = true`; this should be refreshed from a clean
  worktree before an actual release artifact is published.
</blockers>

<context>
The repository is in a “stop coding, start release prep” state. The meaningful
technical work is done for the bounded v1 scope. If work resumes, do not reopen
methodology unless new evidence appears. The highest-signal next action is
operational: clean benchmark rerun, release branch/tag prep, and release-note
packaging. Keep the claim set narrow and honest: one guaranteed
Abacus-reference row, one bounded native holiday-bearing row, and explicit
unsupported surfaces.
</context>

<next_action>
Start with: inspect `docs/src/release.md` and `benchmark/results/reference_machine.*`,
then rerun the frozen benchmark suite from a clean worktree and prepare the
release branch/tag workflow from that narrowed claim set.
</next_action>
