---
phase: 13-prediction-state-and-contract-remediation
task: 0
total_tasks: 6
status: planned
last_updated: 2026-04-24T13:58:17.938Z
---

<current_state>
Phase 13 has been added and planned, but implementation has not started.
Release branch/tag work is intentionally blocked until this phase closes.

Current authoritative plan:
`.planning/phases/13-prediction-state-and-contract-remediation/PLAN.md`

The phase exists to remediate the concrete findings from:
`.planning/reviews/code-review-v4.md`
</current_state>

<completed_work>

- Added Phase 13: Prediction-State and Contract Remediation.
- Added new requirement `PRED-01`.
- Updated `ROADMAP.md`, `STATE.md`, `PROJECT.md`, `REQUIREMENTS.md`, and
  `GSD-BOARD.md` so release prep is blocked behind Phase 13.
- Split Phase 13 into six subphases:
  `13-01` fitted feature-state contract,
  `13-02` trend-state remediation,
  `13-03` holiday exposure-state remediation,
  `13-04` media input contract,
  `13-05` pipeline YAML contract,
  `13-06` revalidation/docs/release gate.
- Asked an explorer subagent to review the plan for underspecification.
- Incorporated the subagent findings into `PLAN.md`, including:
  exact trend-state fields,
  exact holiday-state fields,
  unfitted `prior_predict(model, new_data)` behavior,
  inclusion of flat `ModelResults` and result save/load paths,
  schema/version expectations for serialized artifacts,
  explicit media nonnegative-domain scope,
  `PanelMMMData` validation scope,
  pipeline top-level YAML allowlist,
  and `optimization` versus `optimisation` behavior.
- Ran `git diff --check` on the edited Phase 13 plan; no whitespace issues.
</completed_work>

<remaining_work>

- Start `13-01`: freeze the concrete fitted feature-state implementation
  contract in code/docs before writing broad fixes.
- Decide final representation for fitted trend and holiday state:
  typed fields on `MMMModelSpec`, typed resolved-state wrappers, or stable
  reserved metadata with equivalent type safety.
- Update serialization schema/version policy before changing persisted artifact
  structs.
- Implement and test `13-02` through `13-05`.
- Run focused regressions and the full final gate in `13-06`:
  `make test`, `make docs`, and `make format-check`.
</remaining_work>

<decisions_made>

- Phase 13 is remediation-only; no HSGP, Dash parity, widened panel
  post-modeling, or new inference backend.
- Prediction-state fixes must cover `fit!`, `predict`, `prior_predict`,
  grouped `InferenceResults`, flat `ModelResults`, deterministic replay,
  model/results save/load, and pipeline validation.
- Trend and holiday invariance claims apply to date-derived design state, not
  media adstock carryover.
- Unfitted `prior_predict(model, new_data)` should use date-derived state
  resolved from `model.data`; if that state cannot be resolved, fail clearly.
- Pipeline YAML should accept only the allowlisted top-level keys. Intentional
  opaque extras require an explicit `extras` key only if Phase 13 adds and
  documents it.
</decisions_made>

<blockers>

- Release branch/tag work is blocked until Phase 13 closes.
- Clean-worktree benchmark rerun remains outstanding for release publishing.
- Existing repository worktree is already dirty with many unrelated planning,
  source, docs, and test changes; do not revert unrelated changes.
</blockers>

<context>
The next session should begin with `13-01`, not implementation of trend or
holiday fixes directly. The important first move is to inspect the actual
spec/artifact/result types and decide the smallest durable home for fitted
feature state. The external review and subagent both pointed at the same risk:
without a precise state contract, trend/holiday fixes can land in one path but
miss prediction, flat results, grouped results, or serialization.
</context>

<next_action>
Start with: read `PLAN.md`, then inspect `src/model/types.jl`,
`src/model/builder.jl`, `src/model/io.jl`, `src/model/results.jl`,
`src/mmm/model.jl`, `src/mmm/trend.jl`, `src/mmm/holidays.jl`, and
`src/inference/results.jl`; then implement `13-01` by freezing the fitted
feature-state contract before touching behavior.
</next_action>
