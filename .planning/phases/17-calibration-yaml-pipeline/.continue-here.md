---
phase: 17-calibration-yaml-pipeline
task: 4
total_tasks: 4
status: phase_complete
last_updated: 2026-07-05T00:00:00.000Z
---

<current_state>
Phase 17 is complete. Tasks 17-01 through 17-04 are landed.
</current_state>

<completed_work>
- Phase 16 is complete.
- Phase 15 landed bounded `TimeSeriesMMM` MCMC calibration likelihood support
  and fixture-backed integration evidence for programmatic constructor inputs.
- Task 17-01 landed public dict/YAML parsing for bounded top-level
  `calibration` blocks into `ModelConfig.extras["calibration"]`.
- Task 17-02 landed constructor threading: parsed calibration reaches
  `TimeSeriesMMM.calibration` unchanged, constructor ambiguity is rejected, and
  `PanelMMM` rejects parsed calibration explicitly.
- Scoped verification passed: `test/model/config.jl`, targeted Runic on
  touched Julia files, and
  `JULIA_PKG_SERVER_REGISTRY_PREFERENCE=eager julia --project=. -e 'using Pkg; Pkg.test(; test_args=["model"])'`
  reporting `Pass 913, Total 913`.
- Task 17-02 scoped verification passed:
  `julia --project=. test/model/builder.jl`,
  `julia --project=. test/model/panel.jl`, and targeted Runic on
  `src/model/builder.jl`, `test/model/builder.jl`, and `test/model/panel.jl`.
- Task 17-03 landed pipeline acceptance: top-level `calibration` YAML is
  accepted, `fit.backend` is MCMC/Turing-only, time-series pipeline
  construction receives parsed calibration, and panel/VI-like calibration is
  rejected before fit.
- Task 17-03 scoped verification passed:
  `julia --project=. test/pipeline/config.jl`,
  `julia --project=. test/pipeline/calibration.jl`, and targeted Runic on
  touched pipeline files.
- Task 17-04 closed user-facing docs, changelog, roadmap, state, and ledger
  guardrails for the bounded calibration YAML/pipeline surface without moving
  the broad calibration row beyond `scaffolded`.
- Task 17-04 verification passed with `make docs`, with the known non-fatal
  `index.html` size warning and deployment skipped outside CI, and
  `git diff --check`.
</completed_work>

<next_step>
Choose the next bounded parity/capability slice. Do not continue calibration
into panel, VI, non-logistic lift-test, Dash/UI, or AI-advisor surfaces without
a separate methodological contract.
</next_step>

<known_gotchas>
- Do not change calibration maths.
- Do not expand pipeline calibration beyond the landed time-series MCMC path.
- Keep `PanelMMM`, VI, non-logistic lift-test calibration, Dash/UI, and
  AI-advisor paths out of scope.
- Use targeted model/config tests by default; do not run the full suite for
  small docs or parser slices.
</known_gotchas>
