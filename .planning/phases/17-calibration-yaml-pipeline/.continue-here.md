---
phase: 17-calibration-yaml-pipeline
task: 3
total_tasks: 4
status: task_17_02_landed
last_updated: 2026-07-05T00:00:00.000Z
---

<current_state>
Phase 17 is in progress. Tasks 17-01 and 17-02 are landed; Task 17-03 is next.
</current_state>

<completed_work>
- Phase 16 is complete.
- Phase 15 landed bounded `TimeSeriesMMM` MCMC calibration likelihood support
  and fixture-backed integration evidence for programmatic constructor inputs.
- Task 17-01 landed public dict/YAML parsing for bounded top-level
  `calibration` blocks into `ModelConfig.extras["calibration"]`.
- Task 17-02 landed constructor threading: parsed calibration reaches
  `TimeSeriesMMM.calibration` unchanged, constructor ambiguity is rejected, and
  `PanelMMM` rejects parsed calibration explicitly.
- Scoped verification passed: `test/model/config.jl`, targeted Runic on
  touched Julia files, and
  `JULIA_PKG_SERVER_REGISTRY_PREFERENCE=eager julia --project=. -e 'using Pkg; Pkg.test(; test_args=["model"])'`
  reporting `Pass 913, Total 913`.
- Task 17-02 scoped verification passed:
  `julia --project=. test/model/builder.jl`,
  `julia --project=. test/model/panel.jl`, and targeted Runic on
  `src/model/builder.jl`, `test/model/builder.jl`, and `test/model/panel.jl`.
</completed_work>

<next_step>
Implement Task 17-03: allow bounded time-series MCMC pipeline YAML to carry
the parsed calibration block into model construction, keep panel/VI-like
pipeline calibration rejected, and add a tiny pipeline smoke proving the fit
artifact contains a resolved `MMMCalibrationSpec`.
</next_step>

<known_gotchas>
- Do not change calibration maths.
- Do not wire pipeline fitting in Task 17-01.
- Keep `PanelMMM`, VI, non-logistic lift-test calibration, Dash/UI, and
  AI-advisor paths out of scope.
- Use targeted model/config tests; do not run the full suite for this parser
  slice.
</known_gotchas>
